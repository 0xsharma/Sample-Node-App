pipeline {
    agent { label 'w62ecs2' }
    
    stages {
        stage('deploy') { 
            steps {
                
                echo "lol2"
                sh "ifconfig"
            }
        }
    }
}
